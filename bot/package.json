{
  "name": "hello-matrix-bot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Matrix bot for syncing Beeper/Matrix messages into WordPress comments.",
  "scripts": {
    "start": "node src/index.js",
    "doctor": "node src/doctor.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/doctor.js && node --check src/gravatar.js && node --check src/identity-store.js && node --check src/matrix-client.js && node --check src/wordpress-client.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
